<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}TO THE TABLE!{{else}}LET'S EAT!{{/if}}
	</button>
	<button class="delete-burger" data-id="{{id}}">DELETE BURGER</button>
</li>
